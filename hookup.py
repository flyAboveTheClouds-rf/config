# this script will create a yaml file containing all the hooks


list_file = 'list.txt'

# create a yaml template file
with open(list_file, 'w') as l:
    for num in range(101, 1000):
        l.write(f"num: {num}\nlink: 
# take each word/line from the 'list.txt' file
# put that word in its proper place in 'vancamp.yml'
